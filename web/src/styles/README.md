# PROVIDERS

## Why this folder?

- Add different CSS Styles inside this folder.

## Conventions inside this folder

- Name your components as `common_header.css` (lowercase, separated with
  underscore if needed).
